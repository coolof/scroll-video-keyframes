# Scrobble video on scroll – An experiment

This demo is built with [Astro](https://astro.build) and vanilla javascript. Code can be found inside `src/index.astro`. The JS isn't very pretty but it works and shouldn't affect the performance. The video files are handled by Netlify Large Media.

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                | Action                                             |
| :--------------------- | :------------------------------------------------- |
| `npm install`          | Installs dependencies                              |
| `npm run dev`          | Starts local dev server at `localhost:3000`        |
| `npm run build`        | Build your production site to `./dist/`            |
| `npm run preview`      | Preview your build locally, before deploying       |
| `npm run astro ...`    | Run CLI commands like `astro add`, `astro preview` |
| `npm run astro --help` | Get help using the Astro CLI                       |